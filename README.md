git init
git add (the folder in question)
usually git add . 
git commit -m "message" 
git push -u (url to repo)
git status
changing something, no need to initialise again 
git status (should tell you if it is modified)
git add . 
git commit -m "what is modified" 
git push -u "url same" 
git status to check on health as and when you do this
